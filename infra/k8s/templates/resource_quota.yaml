apiVersion: v1
kind: ResourceQuota
metadata:
  name: store-quota
  namespace: { { namespace } }
spec:
  hard:
    requests.cpu: "1000m"
    requests.memory: "1Gi"
    limits.cpu: "2"
    limits.memory: "2Gi"
    pods: "10"
